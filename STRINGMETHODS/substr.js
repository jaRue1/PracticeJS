//Extracts the characters from a string, beginning at a specified start position, and through the specified number of character
let str = "Ruu love to code !";
let res = str.substr(2);

console.log(res)
// Output
// u love to code !