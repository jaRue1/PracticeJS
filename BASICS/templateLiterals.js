// Template literals 
let firstName = 'Rue Johnson'
const homeTown = 'Fort Lauderdale'
console.log(`Hello My Name is ${firstName}. I'm from ${homeTown}.`)

// Combine Ternary and Template Literal
let str =  `How are you doing ${(firstName) ? firstName : 'No Name'}`
console.log(str)
